{{ form_start(form) }}
    
    {{ form_widget(form) }}
    
    <button class="btn btn-outline-primary"><i class="fas fa-marker"></i> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
